<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Demo Socket.io</title>
    <script src="socket.io/socket.io.js"></script>
    <script src="jquery.js"></script>
    <script>
        //Goi dien len Server
        var socket = io("http://localhost:3000");
        //Client lang nghe io.socket.emit
        socket.on("Server-send-data",function(data){
            // $("#noidung").append(data);
            // console.log(data);
            document.getElementById("noidung").innerHTML = "Humidity: " + data.humidity + " " + "Temperature: "+ data.temperature;
            // socket.emit("client-send-data",{ my: 'data' });
        });
        $(document).ready(function(){
            //alert("hello jquery");
            // $("#mrA").click(function(){
            //     socket.emit("client-send-data",7);
            // });
        })
    </script>
</head>
<body>
    <h1>Hello</h1>
    <div id="mrA">Click Send</div>
    <h2 id="noidung"></h2>
</body>
</html>